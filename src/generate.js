const Web3 = require("web3")
const web3 = new Web3(Web3.givenProvider || "ws://localhost:8545");

// gets `num` accounts with at least `bal` balance (in wei)
function getAccountsWithMinBalance(num, bal) {

}